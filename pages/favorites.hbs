<!DOCTYPE html>
<html lang="ru">
    <head>
        <link href="../favorites.css" rel="stylesheet" type="text/css">
        <script>
            function Play(id){
                const aud = document.getElementById("ms" + id[id.length - 1])
                if (aud.paused) aud.play()
                else aud.pause()
            }
        </script>
    </head>
    
    <body class="back">
        <a href="/main/returnToMain"><img class='strel' src="../Стрелка.png"></a>
        <div id="add_el"></div>
        <div></div>
         {{#each items}}
            <div class="im" id={{this.pict_id}} onclick="Play(id)">
                <img src={{this.pict}}>
                <audio controls src={{this.mus}} id={{this.mus_id}}></audio>
            </div>
            <br>
            <div></div>
         {{/each}}
    </body>
    
</html>
